// 연습문제 1
        // 객체명 : 구름
        // 속성 : 이름('구름'), 나이(6), 종족(강아지)

        //JSON 이용해서 객체를 문자열로 출력해 보세요

        const 구름 = {
            이름: '구름',
            나이: 6,
            종족: '강아지'
        }

        console.log(JSON.stringify(구름));